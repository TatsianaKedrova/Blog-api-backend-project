export type URIParamsRequest = {
  /**
   * URI param id
   */
  id: string;
};

export type URIParamsCommentId = {
  /**
   * URI param commentId
   */
  commentId: string;
};

export type URIParamsPostId = {
  /**
   * URI param postId
   */
  postId: string;
};
